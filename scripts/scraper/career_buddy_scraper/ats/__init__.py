"""ATS adapters: Greenhouse, Lever, Ashby, Workable.

Each adapter implements ``Adapter`` protocol from ``base.py``: detect a careers
URL, fetch the public job-board JSON, normalize each row to ``CanonicalJob``.
"""
